package ru.nsu.basargina.model.fieldsegments;

/**
 * Class that represents particular obstacle segment on the field.
 */
public class ObstacleSegment extends Segment {
    /**
     * Create obstacle segment with given coordinates.
     *
     * @param x x coord
     * @param y y coord
     */
    public ObstacleSegment(int x, int y) {
        super(x, y);
    }
}
