package ru.nsu.basargina.model.fieldsegments;

/**
 * Class that represents particular food segment on the field.
 */
public class FoodSegment extends Segment {
    /**
     * Create food segment with given coordinates.
     *
     * @param x x coord
     * @param y y coord
     */
    public FoodSegment(int x, int y) {
        super(x, y);
    }
}
